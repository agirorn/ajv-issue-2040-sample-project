{
  "name": "ajv-test-ground",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn --silent tsc",
    "clean": "rm -rf dist",
    "dev": "yardman src __tests__ package.json itsconfig.json 'yarn dev:exec'",
    "dev:exec": "clear && yarn build && yarn test",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.7",
    "typescript": "^4.7.4",
    "yardman": "^0.0.9"
  },
  "dependencies": {
    "ajv": "^8.11.0"
  }
}
